# Firebase Configuration
# Lấy config từ Firebase Console > Project Settings > General > Your apps
# Lưu ý: Tất cả biến phải có prefix VITE_ để Vite expose chúng

VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# Hướng dẫn setup:
# 1. Copy file này thành .env (không commit .env vào git)
# 2. Thay thế các giá trị your_* bằng config thực từ Firebase
# 3. Restart dev server sau khi thay đổi .env
